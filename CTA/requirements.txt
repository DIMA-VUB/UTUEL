# CTA — Column Type Annotation dependencies

# Core deep learning
torch>=2.0.0
transformers>=4.40.0
pytorch-lightning>=2.0.0
accelerate>=0.27.0

# LangChain embedder backends (install the ones you use)
langchain-core>=0.3.0
langchain-huggingface>=0.0.3  # HuggingFace local/Hub embeddings (default)
langchain-ollama>=0.2.0       # Ollama backend
langchain-openai>=0.1.0       # OpenAI text-embedding-3-large

# Config / CLI
hydra-core>=1.3.0
omegaconf>=2.3.0
python-dotenv>=1.0.0

# HPO sweep
hydra-optuna-sweeper>=1.2.0
optuna>=3.0.0

# Numerical
numpy>=1.24.0

# Optional: experiment tracking
# tensorboard>=2.14.0
# wandb>=0.16.0
