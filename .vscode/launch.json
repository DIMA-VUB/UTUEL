{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run UTUEL Pipeline",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "env": {
                "OLLAMA_NUM_PARALLEL": "4",
                "OLLAMA_MAX_LOADED_MODELS": "4",
                "OLLAMA_MAX_QUEUE": "2048"
            }
        }
    ]
}
