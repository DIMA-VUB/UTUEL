# ── PyTorch  (CUDA 12.4 wheels — compatible with CUDA 12.5 runtime) ──────────
# NVIDIA-SMI 555.42.06 · Driver 555.42.06 · CUDA runtime ≤ 12.5
# cu124 requires driver ≥ 550  ✓   cu126 requires driver ≥ 560  ✗
# Install command:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

# ── Core ML ───────────────────────────────────────────────────────────────────
pytorch-lightning==2.5.0
transformers==4.48.0
accelerate==1.3.0
sentence-transformers==3.4.1   # local HuggingFace embedder (all-MiniLM-L6-v2)

# ── LangChain embedder backends (install the ones you use) ───────────────────
langchain-core>=0.3.33,<0.4.0
langchain-ollama==0.2.3        # Ollama (default backend)
langchain-openai==0.3.3        # OpenAI text-embedding-3-large
langchain-huggingface==0.1.2   # HuggingFace Hub embeddings

# ── Config / CLI ──────────────────────────────────────────────────────────────
hydra-core==1.3.2
omegaconf==2.3.0
hydra-optuna-sweeper==1.2.0   # hydra --multirun with Optuna TPE/CMA-ES
optuna==3.6.1                  # Bayesian hyperparameter optimisation

# ── Graph-based SMP walks ─────────────────────────────────────────────────────
networkx==3.4.2

# ── Numerical / visualisation ─────────────────────────────────────────────────
numpy==1.26.4
scikit-learn==1.6.1            # t-SNE in dev_check.ipynb
matplotlib==3.10.0

